# Port sur lequel le serveur s'exécute (par défaut: 3000)
PORT=3000

# URL de base pour l'application (utilisée pour CORS si nécessaire)
BASE_URL=http://localhost:3000

# Taille maximum des fichiers uploadés (en bytes, par défaut: 10MB)
MAX_FILE_SIZE=10485760

# Dossier pour stocker les images uploadées
UPLOADS_DIR=uploads